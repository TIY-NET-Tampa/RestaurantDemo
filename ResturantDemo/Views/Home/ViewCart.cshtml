@model ResturantDemo.Models.Order

@{
    ViewBag.Title = "ViewCart";
}

<h2>ViewCart</h2>

<div>
    <h4>Order</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.TimeCreated)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TimeCreated)
        </dd>
    </dl>
    @if (Model.Items == null)
    {
        <div>Nothing in your cart. @Html.ActionLink("Add some?", "Index")</div>
    }
    else
    {
        foreach (var item in Model.Items)
        {
            <div>@item.Name</div>
            <div>@Html.DisplayFor(model => item.Price)</div>
        }
    }


    <br />
    <div>
        @Html.DisplayFor(model => model.TotalPrice)
    </div>

</div>
<p>
    @Html.ActionLink("Place Order", "PlaceOrder") |
    @Html.ActionLink("Back to List", "Index")
</p>
